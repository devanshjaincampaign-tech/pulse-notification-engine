# --- App ---
NODE_ENV=development
PORT=3000

# --- PostgreSQL ---
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=pulse_notifications
DB_HOST=postgres
DB_PORT=5432

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

JWT_SECRET=changeme

# Must be set to your real frontend's domain in production — never leave unset or wildcard in prod
CORS_ORIGIN=